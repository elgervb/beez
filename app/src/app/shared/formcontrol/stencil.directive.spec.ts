import { StencilDirective } from './stencil.directive';

describe('StencilDirective', () => {
  it('should create an instance', () => {
    const directive = new StencilDirective();
    expect(directive).toBeTruthy();
  });
});
